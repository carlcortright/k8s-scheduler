apiVersion: v1
kind: Pod
priority: 9001
metadata:
  name: basic-test
  namespace: custom-scheduler-namespace
spec:
  schedulerName: custom-scheduler
  containers:
    - name: app
      image: nginx:alpine