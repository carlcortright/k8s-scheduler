apiVersion: v1
kind: Pod
metadata:
  name: priority-test
  namespace: custom-scheduler-namespace
spec:
  schedulerName: custom-scheduler
  priorityClassName: high-priority
  containers:
    - name: app
      image: nginx:alpine