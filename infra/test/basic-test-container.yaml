apiVersion: v1
kind: Pod
metadata:
  name: basic-test
  namespace: custom-scheduler-namespace
spec:
  schedulerName: custom-scheduler
  priorityClassName: low-priority
  containers:
    - name: app
      image: nginx:alpine